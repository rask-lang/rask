# validation — pre-orelse snapshot

Byte-identical copy of `examples/validation/` before the migration to the settled `try`/`orelse`
family (#565/#573/#574). This one parses and type-checks with the current compiler; the live dir
targets the spec and won't until the implementation lands. Delete this dir when it does.
