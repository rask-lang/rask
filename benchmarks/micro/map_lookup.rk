// SPDX-License-Identifier: (MIT OR Apache-2.0)

func fill_map() -> Map<i64, i64> {
    mut m = Map.new()
    mut i = 0
    while i < 10000 {
        m.insert(i, i * 2)
        i = i + 1
    }
    return m
}

benchmark "map lookup 10k" {
    mut m = fill_map()
    mut sum = 0
    mut i = 0
    while i < 10000 {
        sum = sum + m.get(i)
        i = i + 1
    }
}
