import http

func forward(request: Request, upstream: string, path: string) -> Response or HttpError {
    let url = "http://{upstream}{path}"
    let response = try http.send_request(request.method, url, request.body, request.headers)
    return response
}
