import http

func forward(request: Request, upstream: string, path: string) -> Response or HttpError {
    const url = "http://{upstream}{path}"
    const response = try http.send_request(request.method, url, request.body, request.headers)
    return response
}
