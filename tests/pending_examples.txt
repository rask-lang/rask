# Examples written in canon syntax that the compiler hasn't implemented yet.
#
# Different from known_fail_examples.txt: those are BUGS (native codegen rot,
# unimplemented stdlib) and have no golden. These are CORRECT — the spec settled
# on a spelling, the sources moved to it, and the compiler is still catching up.
# They keep their golden in tests/golden/, because the expected output never
# changed; only the syntax did.
#
# tests/examples_gate.sh treats a failure here as expected. If one starts
# PASSING, the gate fails on purpose — the feature landed, so delete its line
# and the example goes back to being enforced forever.

# (empty — `is none` landed in the compiler, so both former entries went back
# to being enforced. The gate told us to remove them, which is the whole point.)
