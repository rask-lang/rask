# TDD backlog: tests that assert SPEC-CORRECT behavior for features that are not
# implemented yet. They are EXPECTED to be red — each one drives the
# implementation of its feature and flips green (harness reports UNEXPECTED PASS
# → promote it out of this file) the moment the feature lands.
#
# Each line ends with `(<backend> <phase>)` — which backends fail and how far
# each gets — and the harness holds the file to it. Unbuilt features are
# `(both check)`: the name doesn't resolve, so neither backend gets past the
# checker. One that starts failing later than that has partly landed.
#
# This is distinct from tests/known_divergences.txt, which tracks BUGS/
# REGRESSIONS in features that are supposed to work. Red here = roadmap; red
# there = something broke.
#
# Format: one suite basename per line, trailing `# <spec-id> — #<issue>`.
# The differential harness treats both files as expected-red (non-fatal).

# --- spec-heavy pending features ---
p08_sequence.rk            # type.sequence function-valued iteration protocol — unimplemented (both check)
p09_simd.rk                # type.simd f32x4 etc. vector types — checks, but no SIMD value is ever built below the checker, #1059 (both check)
p10_binary.rk              # type.binary @binary — the interpreter builds and parses; native generates neither method. Blocked on #1058: G1's `[]u8` signature can't be generated as a real method while a Vec<u8> can't be passed to one (native compile)
p11_gradual_generalization.rk # type.gradual IN1/IN3 — an inferred parameter never generalizes, so the first call site pins it, #904 (both check)

# --- declared-but-unbuilt stdlib surface ---
t_week_collection_stubs.rk # std.collections — 11 declared Vec/Map methods unimplemented on both backends, #912 (both check)
t_week_range_adapters.rk   # ctrl.ranges — a range has no terminals or adapters; routed through the unbuilt sequence protocol, #920 (both check)
