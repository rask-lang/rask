// SPDX-License-Identifier: (MIT OR Apache-2.0)
// A bare literal has to fit the type it's asked to be. An unpinned literal var
// used to bind to whatever it met first, so this type-checked.

func f() -> string {
    return 1
}

func main() {
    println(f())
}
