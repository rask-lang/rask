# Suite files with a KNOWN, TRACKED interp/native divergence or shared bug.
# The differential harness (tests/differential.sh) runs these on both backends,
# reports them as KNOWN-FAIL (non-fatal), and FAILS if one silently starts
# passing (prune it) or a NEW untracked red file appears.
#
# Format: one basename per line. Trailing `# <cluster> — #<issue> (<backend>)`.
# When a bug is fixed, delete its line so the file rejoins the green gate.
#
# Direction is noted because divergence goes both ways: native rot AND interp
# lag both count. See docs/TESTING.md for the full map.

# --- native codegen bugs (interp correct) ---
t19b_union_method_dispatch.rk # native union error message() dispatch + i32 or (A|B) codegen — #375, rask#211 (native)
# (os.pid/env_vars native, Cell/Mutex, and math moved to pending_features.txt —
#  they are "backend not built", not regressions)

# --- interpreter lag (native correct) ---

# --- shared / both-backend bugs ---
t_parse_float.rk # `"3.5".parse()` with the target from the binding always misses; only the turbofish form works — #610 (both)

# --- new regression witnesses (this session) ---

# --- E0361: binding whose type inference left open (#620) ---
# Not a divergence — these fail `check` on both backends now that the type
# checker reports an unresolved binding instead of letting lowering guess i64.
# Every one is an expression form that doesn't propagate its value type:
# `with`-block-as-value, `select`-as-value, `loop`-as-value, iterator `collect`,
# `??` taking its type from the default. Fix the inference and delete the line —
# the annotation the error suggests works meanwhile, but these are compiler
# bugs, not programs that need annotating.
t20_control_expressions.rk # `x * 2` on two unsuffixed literals — #630, not #620 (both)
t21_let_bindings.rk        # `x + 1` on two unsuffixed literals — #630 (both)
t23_with_blocks.rk         # #630 (both)
t25_iterator_adapters.rk   # `x * 2` in a map closure, two unsuffixed literals — #630, not #620 (both)
t48_loops.rk               # `attempt * 10` on two unsuffixed literals — #630, not #620 (both)
t_inferred_return_type.rk  # #630 (both)
t_with_mutex_value.rk      # #630 (both)
