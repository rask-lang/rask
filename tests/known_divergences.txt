# Suite files with a KNOWN, TRACKED interp/native divergence or shared bug.
# The differential harness (tests/differential.sh) runs these on both backends,
# reports them as KNOWN-FAIL (non-fatal), and FAILS if one silently starts
# passing (prune it) or a NEW untracked red file appears.
#
# Format: one basename per line. Trailing
# `# <cluster> — #<issue> (<backend> <phase>)`, where backend is native, interp
# or both and phase is check, compile or run — how far that backend gets before
# it fails. The harness checks the file still fails that way, so a line that
# stops matching is MISFILED rather than quietly still-red; see the header of
# differential.sh and #1005.
# When a bug is fixed, delete its line so the file rejoins the green gate.
#
# Direction is noted because divergence goes both ways: native rot AND interp
# lag both count. See docs/TESTING.md for the full map.

# --- native codegen bugs (interp correct) ---

# (os.pid/env_vars native, Cell/Mutex, and math moved to pending_features.txt —
#  they are "backend not built", not regressions)
#
# Pruned 2026-08-18, fixed and re-verified on native before removing the line:
#   t_i128_arithmetic.rk — #762. `MirType::I128`/`U128` at 16 bytes, mapped to
#     Cranelift's own `I128`. Add and sub lower there already; multiply,
#     divide and remainder have no rule, so they go through runtime helpers
#     that hand back a status the caller turns into the usual overflow panic.
#     All 6 tests pass on both backends.
#
# Pruned 2026-08-17, fixed and re-verified on native before removing the line:
#   t19b_union_method_dispatch.rk — #776. A union err payload now carries its
#     member index, so `r is ParseError` tests the member instead of the Result's
#     own tag, and `e.message()` switches on the index to reach the member's own
#     method. All 4 tests pass on both backends.
#
# Pruned 2026-08-08, both fixed and re-verified on native before removing the line:
#   t_try_coalesce_diverge.rk — #635, which turned out not to be the `continue` at
#     all: a `return` at the end of any loop body was being discarded.
#   t_pool_iter_handles.rk    — #652, handles from `for h in pool` rejected by the
#     same pool. All 5 tests pass on both backends now.

# --- interpreter lag (native correct) ---
# (empty)
#
# Pruned 2026-08-26, fixed and re-verified on both backends before removing the line:
#   t_month_unsafe.rk — #935. The interpreter had no pointer value: `as_ptr()`
#     handed back the integer 0, `*p` folded to 0, and `read`/`offset` were
#     "no method on `i64`". It now carries a real pointer — the buffer it came
#     from plus an element index — so pointer arithmetic, reads, writes and
#     identity all answer what native answers. The file grew from 6 tests to 16
#     while it was open; all 16 pass on both backends.

# --- shared / both-backend bugs ---
# (t_parse_float was the previous one, fixed in #610: a float payload sits in a
#  Result slot as an f64, and the parse adapter was writing it at the declared
#  width, so an f32 target read four bytes of eight.)

# --- new regression witnesses (this session) ---
#
# Empty. The 2026-08-19 coverage sweep's ten probes are all fixed and pruned
# (2026-09-04), and the one entry that turned out not to be a bug moved to
# pending_features.txt:
#
#   t_day_const_string_array.rk     — #1000. A string in an array was stored as a
#     pointer to wherever the literal was built. In a normal run the const's
#     initializer is inlined into `main`, so those pointers aimed at a live
#     frame; in a test binary the init thunk returns first and every element
#     pointed into dead stack. Strings live in the element slot now.
#   t_array_string_join.rk          — #1021. An array receiver fell through to
#     the shared `Vec` lowering, which reads a header an array hasn't got.
#   t_month_param_modes.rk          — #899. `mutate` on a scalar wrote into a
#     spilled copy nobody read back. Also a spec change: the edge-case row
#     saying a Copy type is copied in contradicted PM2, and PM2 won.
#   t_month_error_union_narrowing.rk — #1002. `r is AErr as e` bound `e` at the
#     whole union, so a call on it switched over every member.
#   t_month_try_in_test.rk          — #932. A test body is a void function, so
#     `try`'s error path returned a value out of a void signature. It ends the
#     test now, saying so.
#   t_month_resource_loop.rk        — #928. Nothing solves types between the
#     statements of a loop body, so "does this method mutate its receiver?" was
#     answered by method *name*. Deferred to after solving.
#   t_week_parse_range.rk           — #919. `parse<T>` kept the 64-bit parse when
#     T came from inference rather than a turbofish, so no width range-checked.
#   t_day_unsigned_to_float.rk      — #974. Cranelift's saturating conversions at
#     i8/i16 lower through the 32-bit sequence; the round-trip check now runs at
#     64 bits, both directions.
#   t_week_gradual_generics.rk      — #905 fixed (an omitted parameter type
#     reached MIR as a void). #904, generalizing an inferred parameter, is
#     unbuilt rather than broken and is now p11_gradual_generalization.rk.
#   t_for_tuple_binding_dispatch.rk — #1022. A `for (i, x)` binding got fresh
#     variables tied to nothing, so the names stayed untyped forever.
#   t_month_reflect_fields_value.rk — #997. Native refused `reflect.fields<T>()`
#     as a value; it builds the vector now.

# --- E0361: binding whose type inference left open (#620) ---
# Empty. All 19 files that lit up when E0361 went on by default now compile.
#
# Worth knowing, because the labels on this list were wrong more often than
# right. Only four of the categories were real:
#
#   `break x` was parsed as a jump, not a value. The parser guessed from the
#   next token, so `break i` became "go to a label called i", the loop's value
#   type had nothing to unify against, and three separate workarounds had grown
#   downstream to paper over it.
#
#   A `select` arm never bound its received value, so the arm body typed `v` as
#   a free variable and that spread to the whole select.
#
#   `?? <literal>` deferred forever. An unsuffixed number never settles on its
#   own — literal defaulting does it, and that runs after the last solve, by
#   which point the deferred constraint is gone.
#
#   Closure parameters were never bound at all. The body worked on a variable
#   nobody could pin, so `map(|r| r.view.title)` couldn't type `r.view`.
#
# The rest were two other bugs wearing E0361's clothes: an operator with a bare
# literal operand (#630, seven files) and a range that carried no element type,
# so `for i in 1..6` left `i` free and every Vec it filled with it. Both
# `with`-block-as-value and `collect` were mislabelled — reduced, neither needed
# a `with` block or a `collect` to fail.
