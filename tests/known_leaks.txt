# Suite files that leak, with the count the leak gate saw when the line was
# written. tests/leak_gate.sh runs every suite file under RASK_LEAK_CHECK=1,
# reports these as expected, and FAILS on a file that starts leaking without a
# line here — so the gate enforces "no NEW leaks" rather than "no leaks".
#
# When a file stops leaking, delete its line. The gate says so by name.
#
# The counts are re-measured when a leak fix lands. 2462 allocations across the
# suite became 2080 with the closure work (64 files improved,
# `t27_own_closure_state.rk` off the list), 1952 once a `Map` in a struct field
# started being freed (four more files clean), 1641 once the vectors the OS
# hands over became the caller's (`t41_os.rk` clean, `t_os_env.rk` 160 → 4),
# 1492 once a struct handed to a callee that doesn't keep it went back to
# being the caller's (ten more files clean, `t_subprocess.rk` among them), and
# 1411 once a group whose death falls on an edge rather than in a block got its
# release there (eight more, `t_iterate_field_collection.rk` among them), and
# 1345 once a container inside a container element was freed with the element
# (seven more, and `t_week_nested_data.rk` halved), and 1319 once a call through
# a closure could say which bodies it reaches (`t_vec_flat_map_sort_by_key.rk`
# 33 → 11), and 1311 once an enum variant's own container field was released
# with the enum, and 1120 once a rack or a pool in a struct field was freed with
# the struct (seven more files clean, and the rack cluster from 252 allocations
# to 70 — `p12_rack_link_churn.rk` alone 128 → 2), and 1054 once the last few
# runtime calls that hand back a fresh Vec were written down as such — nine
# more clean, `rack.nodes()` being the one that was called in a loop —, and 785
# once a channel's two ends were released with the frame that made them (ten
# more clean, `t_select.rk` 74 → 0), and 729 once a box in a struct field was
# released with the struct (`t_io_buffer.rk` 42 → 26 — an `io.Buffer` keeps its
# read position in a `Shared<i64, Local>`), and 701 once a value taken away on
# one path was still released on the others (`t_string_from_utf8.rk` 26 → 0 —
# `string.from_utf8` rejects from eight places and leaked its builder from every
# one), and 629 once `split`, `lines` and `split_whitespace` could join that
# list at all — what had kept them off it was a string read out of one container
# and pushed into another, which took no reference (nine more files clean), and
# 594 once a rack node's own strings and containers were freed with the node
# (`t_rack_stale_records.rk` 15 → 2, `p13_rack_snapshot.rk` 21 → 10), and 521
# once a container captured into a closure the frame also drops was freed by
# the frame rather than by glue no site could agree on — the sequence-adapter
# family from 152 allocations to 88 —, and 498 once a closure handed back
# *through* a closure was owned by its caller (`t41_closure_environment_drop.rk`
# 28 → 10, which was the per-element half of #1045), and 496 once a container
# whose *elements* are enums could be described at all — a guard entry per
# variant, which the suite barely exercises and a decoded document is made of —,
# and 490 once a container held behind a `?` or an `or` was released with the
# value holding it, and 453 once MIR could say which container a wrapper's
# payload is (#1140), a runtime helper that uses a callback up was written down
# as not keeping it, and a container made inside one arm of a branch got a free
# where control leaves that arm (five more files clean, `t_json_decode.rk`
# 22 → 7), and 377 once one allocation under several names got freed once
# instead of not at all — twelve more files clean, and that was the whole of
# `t_week_closures.rk`, `t_week_ranges.rk` and `t_week_sorting.rk`.
# `RASK_LEAK_TRACE=1` says which runtime function is still holding what, which
# is how each cause was found — `docs/leak-tracing.md` has the recipe.
#
# Why this file went from empty to 180 lines in one commit: the gate decided by
# grepping the output of `rask test` for the leak report, and `rask test` was
# reading the test binary's stderr and dropping it. The report never reached the
# grep, so every file read as clean. Forwarding that stderr is what made these
# visible; none of them are new (#1048).
#
# A passing `assert` on a heap string took 33 files off this list at once. The
# release was emitted on the branch that prints the strings and aborts, so the
# path that continues never ran one — every passing string assert leaked a
# buffer, and asserts are everywhere in a test suite (#1049).
#
# An adapter chain now frees the environments it built, which is most of what
# the sequence files were carrying — t41 went from 212 allocations to 12, and
# `.map()` in a loop from 200 to none (#1045).
#
# What's left, after that and after wiring Rack and Pool into the drop pass:
#   - a container captured by a closure, or nested inside another container, is
#     never freed (#1035) — the biggest remaining share. A container in a
#     *struct field* is freed now (#1120): the handle a field read hands back
#     joins the struct's value group, so liveness knows the two die together
#   - a callback that builds a value per element — flat_map's inner sequences
#     and containers — hands back something nobody owns (#1045, the half this
#     didn't reach)
#   - a closure made on one branch of an `if` has no point where freeing it is
#     unconditionally safe (#1045)
#
# Eight files came off in one go when module-level consts started being freed:
# a const has no scope to end, so the drop pass had nowhere to put a release,
# and the frees now happen in a generated `rask_const_free` the runtime calls
# after main returns and after detached tasks are awaited (#1116). That covers
# the containers and boxes a const holds, and the heap block a `string` or
# struct const's slot points at.
#
# Format: one basename per line, then `# N allocations`.



# The seventeen added 2026-09-05 came in with the bug-crunch branch: some
# are files that branch added, the rest leak there for shapes the fixes
# above didn't reach. None are new leaks — they were on that branch's own
# list before the merge.

p07_heap.rk  # 7 allocations
p12_rack_link_churn.rk  # 2 allocations
p13_rack_snapshot.rk  # 10 allocations
p21_sequence_adapters.rk  # 2 allocations — the adapters' closure environments and the containers they hold (#1045's tail)
t26_custom_sequence.rk  # 4 allocations
t29_branch_merge.rk  # 7 allocations
t34_vec_as_sequence.rk  # 1 allocation — the source closure captures self, #1045
t35_returned_sequence.rk  # 2 allocations — a returned chain's captured containers, #1035
t38_lazy_collection_adapters.rk  # 6 allocations — a collection's adapters capture the collection, #1035
t39_loop_backedge_container_free.rk  # 10 allocations — a container in a struct field and a map's own storage, #1047
t40_forwarded_generic_method.rk  # 14 allocations — sequence adapter closures, #1045
t40_stdlib_renames.rk  # 3 allocations
t41_closure_environment_drop.rk  # 10 allocations — a closure made on one branch has nowhere safe to be freed, #1045
t42_flatten_shaped_header.rk  # 9 allocations — flatten's per-element inner sequences, #1045
t62_trait_object_positions.rk  # 3 allocations
t65_vec_of_trait_objects.rk  # 10 allocations
t_aggregate_returns.rk  # 2 allocations — the `Vec<any Describes>`, which is the
t_any_error_boxing.rk  # 5 allocations
t_atomic_across_tasks.rk  # 6 allocations
t_bare_error_type.rk  # 9 allocations — same trait-box leak as the line above
t_bare_lock_value.rk  # 3 allocations
t_borrow_param_consume.rk  # 2 allocations
t_box_replace_payload.rk  # 3 allocations
t_capture_freed_with_its_closure.rk  # 2 allocations — the string chain's per-element sequences, #1045
t_channel_spawned_producer.rk  # 3 allocations — `own` copies a sender into every task without taking a reference, #1139
t_closure_call_container.rk  # 24 allocations — the source vector each
t_closure_diverging_return.rk  # 2 allocations
t_closure_in_collection.rk  # 9 allocations
t_collection_api_gaps.rk  # 8 allocations
t_container_in_a_wrapper.rk  # 1 allocation — the `cstring` case, whose callee is
t_container_ownership.rk  # 1 allocation
t_cstring_ffi.rk  # 4 allocations — the terminated copy comes back inside a
t_debug_renders_containers.rk  # 2 allocations
t_element_type_resolution.rk  # 4 allocations
t_fs_open_failure.rk  # 2 allocations
t_io_buffer.rk  # 21 allocations
t_io_seek.rk  # 21 allocations — the `io.Buffer` locals, the same shape `t_io_buffer.rk` carries
t_json_decode.rk  # 7 allocations
t_local_box_payload.rk  # 1 allocation
t_module_qualified_variants.rk  # 5 allocations — trait boxes again
t_month_atomics.rk  # 8 allocations
t_month_borrowing.rk  # 1 allocation
t_month_boxes.rk  # 8 allocations
t_month_panic_observed.rk  # 9 allocations
t_month_stdlib_modules.rk  # 7 allocations
t_mutex_payload_kinds.rk  # 10 allocations
t_net_loopback.rk  # 5 allocations
t_os_env.rk  # 4 allocations
t_package_state.rk  # 6 allocations
t_rack_stale_records.rk  # 2 allocations
t_random_seeded.rk  # 10 allocations
t_runtime_handle_out_of_result.rk  # 1 allocation
t_shared_box_freed.rk  # 2 allocations — the `s.clone()` handle, deliberately not freed
t_shared_inline_access.rk  # 1 allocation
t_shared_strategies.rk  # 3 allocations
t_spawn_join_result.rk  # 2 allocations
t_string_builder_mutate.rk  # 1 allocation
t_string_rc_escapes.rk  # 7 allocations
t_string_unicode.rk  # 3 allocations
t_supertrait_dispatch.rk  # 2 allocations
t_trait_object_in_field_container.rk  # 7 allocations
t_trait_object_optional_payload.rk  # 6 allocations
t_vec_element_returns.rk  # 7 allocations
t_vec_field_filter_map.rk  # 4 allocations
t_week_imports.rk  # 2 allocations
t_week_opaque_struct_fields.rk  # 1 allocation
t_week_traits.rk  # 5 allocations
t_with_collection_field_writes.rk  # 6 allocations
                        # same trait-box leak as the two lines below. Nothing to
                        # do with the aggregate return the file is named for:
                        # every other test in it is clean.
                            # chain captures (#1035's closure half), and
                            # the `SHARED` paths on purpose: one closure
                            # handing back a const's vector rejects the
                            # whole callee set, which is the point of the
                            # third test
                             # small enough to inline, so there is no call left
                             # to read a handoff off (#1117's other half)
                  # `cstring or NullByteError`, and a container returned in a
                  # `T or E` is freed by nobody (#1117)
                             # field (#1035, #1047); the clones themselves are freed
                   # the Vec/Pool of structs holding strings (#1035)
