# Suite files that leak, with the count the leak gate saw when the line was
# written. tests/leak_gate.sh runs every suite file under RASK_LEAK_CHECK=1,
# reports these as expected, and FAILS on a file that starts leaking without a
# line here — so the gate enforces "no NEW leaks" rather than "no leaks".
#
# When a file stops leaking, delete its line. The gate says so by name.
#
# The counts are re-measured when a leak fix lands. 2462 allocations across the
# suite became 2080 with the closure work (64 files improved,
# `t27_own_closure_state.rk` off the list), 1952 once a `Map` in a struct field
# started being freed (four more files clean), 1641 once the vectors the OS
# hands over became the caller's (`t41_os.rk` clean, `t_os_env.rk` 160 → 4),
# 1492 once a struct handed to a callee that doesn't keep it went back to
# being the caller's (ten more files clean, `t_subprocess.rk` among them), and
# 1411 once a group whose death falls on an edge rather than in a block got its
# release there (eight more, `t_iterate_field_collection.rk` among them), and
# 1345 once a container inside a container element was freed with the element
# (seven more, and `t_week_nested_data.rk` halved), and 1319 once a call through
# a closure could say which bodies it reaches (`t_vec_flat_map_sort_by_key.rk`
# 33 → 11), and 1311 once an enum variant's own container field was released
# with the enum, and 1120 once a rack or a pool in a struct field was freed with
# the struct (seven more files clean, and the rack cluster from 252 allocations
# to 70 — `p12_rack_link_churn.rk` alone 128 → 2), and 1054 once the last few
# runtime calls that hand back a fresh Vec were written down as such — nine
# more clean, `rack.nodes()` being the one that was called in a loop —, and 785
# once a channel's two ends were released with the frame that made them (ten
# more clean, `t_select.rk` 74 → 0), and 729 once a box in a struct field was
# released with the struct (`t_io_buffer.rk` 42 → 26 — an `io.Buffer` keeps its
# read position in a `Shared<i64, Local>`), and 701 once a value taken away on
# one path was still released on the others (`t_string_from_utf8.rk` 26 → 0 —
# `string.from_utf8` rejects from eight places and leaked its builder from every
# one), and 629 once `split`, `lines` and `split_whitespace` could join that
# list at all — what had kept them off it was a string read out of one container
# and pushed into another, which took no reference (nine more files clean), and
# 594 once a rack node's own strings and containers were freed with the node
# (`t_rack_stale_records.rk` 15 → 2, `p13_rack_snapshot.rk` 21 → 10), and 521
# once a container captured into a closure the frame also drops was freed by
# the frame rather than by glue no site could agree on — the sequence-adapter
# family from 152 allocations to 88 —, and 498 once a closure handed back
# *through* a closure was owned by its caller (`t41_closure_environment_drop.rk`
# 28 → 10, which was the per-element half of #1045), and 496 once a container
# whose *elements* are enums could be described at all — a guard entry per
# variant, which the suite barely exercises and a decoded document is made of —,
# and 490 once a container held behind a `?` or an `or` was released with the
# value holding it, and 453 once MIR could say which container a wrapper's
# payload is (#1140), a runtime helper that uses a callback up was written down
# as not keeping it, and a container made inside one arm of a branch got a free
# where control leaves that arm (five more files clean, `t_json_decode.rk`
# 22 → 7), and 377 once one allocation under several names got freed once
# instead of not at all — twelve more files clean, and that was the whole of
# `t_week_closures.rk`, `t_week_ranges.rk` and `t_week_sorting.rk`, and 347
# once a value handed over on one path could still be released on the others —
# `try` on a `Container or E` inside a function that returns `_ or E` reads the
# error out of the wrapper and stores it into the Result going back, and the
# container on the wrapper's *ok* side went with it (`t_io_seek.rk` 21 → 0), and
# 343 once passing a trait object to a function that borrows it stopped counting
# as handing it over (`t_io_buffer.rk` 21 → 17; covering the containers inside a
# boxed value as well was tried, double-freed, and backed out — #1144), and 322
# once a `Random` had a free at all: it is a heap block behind an opaque handle
# and `rask_rng_free` did not exist, so every `Random.from_seed(n)` leaked its
# state (three more files clean), and 297 once a box gave back a container it
# holds: `Shared.mutex(Map.new())` moves the map in, so the map's free belongs
# to whoever drops the box's last reference — which only the runtime knows, so
# the payload kind travels to the constructor and lives on the box (four more
# files clean), and 287 once `Rack.snapshot()` stopped cloning a node's
# `Vec`/`Map` field twice and dropping the first copy (`p13_rack_snapshot.rk`
# 10 → 0), and 279 once a function that hands back a fresh trait box made its
# caller the owner — `return Boom.Bad` in a `-> i64 or Error` boxes the enum,
# and every failing `or Error` call leaked the box (four files improved).
#
# The rest of that one was #1147, and it is closed: 274 once a trait box got
# dropped where control leaves the region its definition rules, the same rule
# `container_drop` already uses — `classify(-1) catch e => -1` binds the error,
# so a name did own the box, but a definition inside a `catch` dominates no
# return, so it had nowhere to be dropped (`t_bare_error_type.rk` 6 → 3) — and
# 270 once the frame could follow a box through a wrapper it holds itself. A
# forwarding function reads the box out of what it called and repacks it into
# its own wrapper, and once that function is inlined the whole trip is one
# frame's: stored into an aggregate, moved with it, read out again under a new
# name. That last name owns the box (`t_any_error_boxing.rk` 4 → 2,
# `t_bare_error_type.rk` 3 → 1), and 268 once a string handed back by a trait
# call got released. MIR has three kinds of call — direct, through a closure,
# through a vtable — and the elision pass's test for "did this frame take this
# reference itself" listed only the direct one, so `n.label()` on a boxed value
# came back with a reference nobody gave back (#1145,
# `t_module_qualified_variants.rk` clean), and 250 once a closure body could
# stop being shared by two chains that disagree. Inlining copies a `flat_map`
# *create* site into each caller and leaves one body behind it, so the capture
# slot holding `f` saw the union over every closure ever passed to `flat_map`
# anywhere — one member handing back somebody else's container made the whole
# set unusable, and the chain next door leaked. Each disagreeing group of
# create sites gets its own copy of the body now (#1146,
# `t_closure_call_container.rk` 24 → 6), and 243 once one vector feeding two
# chains got one free rather than none. A capture is an escape, so the source's
# free rides along with the closure — and that counts capturers by local, so
# two inlined copies of a chain each looked like the only one and both freed
# it. Refusing both was the first answer and it leaks; the free goes after the
# last of the closure drops instead (#1148, `t42_flatten_shaped_header.rk`
# 9 → 3, `t34_vec_as_sequence.rk` clean), and 223 once a struct handed to a
# closure the frame also drops stayed the frame's. Any capture read as handing
# the aggregate away, so a sequence built over a struct's `Vec` field left the
# container to nobody — every `h.walk()` shape. The closure is a name that
# reaches the group now, the way a handle read out of one is: alive for
# placement, never the name released (`t40_forwarded_generic_method.rk`,
# `t26_custom_sequence.rk` and `t35_returned_sequence.rk` all clean), and 209
# once a closure made on one branch inside a loop got a drop at all — neither
# the back edge nor the return dominates the arm that made it, which
# `t41_closure_environment_drop.rk` had written down as unfixable and wasn't.
# The rule is `container_drop`'s and now lives in `analysis::drop_sites`,
# because three passes wanted it (`t41` clean,
# `t_closure_call_container.rk` 6 → 2), and 199 once a copy made inside a loop
# stopped counting as the end of its source. `for x in v` copies `v` into the
# body every turn, so `v` is live out of the block that copies it — reading
# that as a move left every name in the group either moved-from or with nowhere
# to put a free, and the container was freed by nobody
# (#1047, `t39_loop_backedge_container_free.rk` clean), and 185 once an
# `Atomic<T>` had a free at all. One machine word behind an opaque handle and
# no `rask_atomic_int_free` in the runtime, so every counter in every program
# leaked eight bytes — the same shape `Random` was in. It hid because
# `into_inner` reads the word and gives the block back in one call, which is
# the shape a test reaches for (`t_month_atomics.rk` and
# `t_atomic_across_tasks.rk` clean), and 180 once `os.env` stopped allocating a
# box for its answer. The runtime heap-allocated a 16-byte string header purely
# so codegen could copy out of it, and nothing freed the header — the string
# goes straight into the option's payload now (`t_os_env.rk` and
# `t_month_stdlib_modules.rk` clean), and 173 once `drop(p)` on a `Heap<T>`
# freed the block. `Heap<T>` is transparent — a reference to one has the
# payload's type, because that is what the program treats it as — so lowering
# asked "is this argument a pointer?", was told "a struct", and emitted no code
# at all. Every `Heap` of a struct leaked its block, and the one shape that
# *did* free was `Heap<i32>`, where there is no block to free
# (`p07_heap.rk` 7 → 2), and 162 once a box could hand its payload back without
# allocating a block for it. `Cell.replace` copied the old value into a fresh
# block so the pointer it returned would outlive the call, and nobody freed the
# block; `Cell.into_inner` was `Cell.get` under another name, so freeing the
# cell would have dangled that pointer and the whole cell stayed behind. Both
# write into the caller's own destination now — six more files clean,
# `t_month_boxes.rk` among them.
#
# And 155 once a string variable written every turn released what it held. By
# the time the reference-counting pass sees it the loop-carried write isn't a
# statement — it's a phi's incoming edge — and the last-use walk looks past it
# from both sides, so `mut junk = ""` rewritten eight times leaked seven
# buffers. The release goes at the end of the back-edge block, where the name
# still points at the value being replaced (`t_string_rc_escapes.rk` clean, and
# `t_vec_element_returns.rk` with it — `Vec.remove`/`Vec.pop` hand an element
# out into a name a loop then writes over, which is the same shape).
#
# And 134 once the frame owned a boxed value's contents rather than the box.
# `TraitBox` copies the value shallowly, so the box and the frame's own local
# hold the same container handle, and two boxes of one value hold it twice — a
# free has to happen exactly once, and the box is not a place where "exactly
# once" can be arranged. Calling the boxing a hand-over left the contents to
# the box's drop glue, which can't do it. `TraitDrop` is to a box what
# `ClosureDrop` is to a closure, so its presence is the answer to "does the
# frame outlive this box" (#1144, `t_io_buffer.rk` 17 → 3).
#
# Two things had to change together for that, and the first hid the second. The
# release was landing on the box's own name — `rc_dec_contents(_40)` on an
# `any Describes`, which the release walk has no case for and quietly does
# nothing about — so the container inside the boxed value leaked exactly as
# before. With the release on the value instead, the box's drop glue turned into
# a double release: it freed the value's *string* fields, right when the box was
# the owner and one decrement too many once the frame is. `TraitBox` never took
# a reference to copy, so the glue and the vtable's drop slot are gone, and a
# box dying frees its block and nothing inside it.
#
# One buffer per runtime-built element came back when a `Vec<string>` literal
# stopped taking a second reference to it. `rask_vec_from_static` is handed a
# temporary array the frame never frees, and lowering has already put a
# reference in its slots — so taking the bytes over takes the reference with
# them, and retaining on top of that leaked one buffer per element. A literal
# element carries a sentinel refcount every release walks away from, which is
# why an all-literal vector was fine and this sat unnoticed.
#
# Nine more when a container started freeing the closure blocks its elements
# point at (#1149). The element descriptor had no kind for a closure, so the
# free walk skipped it and a `Vec<func>` left one block per element. A block
# describes itself — its size and its environment-drop glue are the header words
# before the pointer — so one kind covers every closure with nothing
# type-specific to generate. The block carries a reference count now, because a
# *derived* container (clone, slice, chunk, concat) copies element bytes and two
# vectors naming one block would free it twice; a deep copy isn't available,
# since the environment's layout is known only to that generated glue and there
# is no `env_retain` to ask. `t_closure_in_collection.rk` 9 → clean.
#
# Eight more when a fused loop reading a struct field started giving the
# field's container back. `r.xs.zip(other)` reads the handle straight off the
# struct, and a bare handle is never the name a release walks — so the release
# belongs on the edge where the group dies, which is the loop's exit block.
# `aggregate_edge_releases` is that mechanism and it asked for the exit to have
# exactly one predecessor, which is the easy case of the rule it wanted. A fused
# `zip` has two ways out, one per side running out, so neither edge qualified.
# Every predecessor having it live on the way in is the same rule stated
# properly. `t_collection_api_gaps.rk` 8 → clean.
#
# Thirty-two more when a container started freeing the `data` half of a trait
# box element's fat pointer (#1149). Nine files' leaks were nothing but those
# blocks — `RASK_LEAK_TRACE` put every one at a boxing site in the test's own
# source, eight bytes apiece, which is `max(concrete_size, 8)`. The block's size
# is the vtable's first word, so this needs no generated glue either, and a
# derived container copies the block for the same reason.
#
# The block only. What a boxed value *holds* is still open: a box inside a
# container has no frame outliving it, and #1144's rule is that a boxed value's
# contents belong to the frame. No suite file boxes a value with a container in
# it, so nothing here measures that half.
#
# Don't write the running total here. This line used to, and it drifted 22 out
# from the sum of the lines below — a fix that emptied a file removed its entry
# without touching the prose, so the tally read like a measurement and was a
# story about a state two commits back. `leak_gate.sh` prints the count and the
# file total on every run, and fails on a line whose number no longer matches
# what the file leaks, which is where that belongs.
#
# Two of the entries are `t_heap_box_dropped.rk`, written for the `drop` fix and
# left on the list because a freed box says nothing about the vector inside its
# payload — a different question from #1144's, which is about `any Trait`.
# `RASK_LEAK_TRACE=1` says which runtime function is still holding what, which
# is how each cause was found — `docs/leak-tracing.md` has the recipe.
#
# Why this file went from empty to 180 lines in one commit: the gate decided by
# grepping the output of `rask test` for the leak report, and `rask test` was
# reading the test binary's stderr and dropping it. The report never reached the
# grep, so every file read as clean. Forwarding that stderr is what made these
# visible; none of them are new (#1048).
#
# A passing `assert` on a heap string took 33 files off this list at once. The
# release was emitted on the branch that prints the strings and aborts, so the
# path that continues never ran one — every passing string assert leaked a
# buffer, and asserts are everywhere in a test suite (#1049).
#
# An adapter chain now frees the environments it built, which is most of what
# the sequence files were carrying — t41 went from 212 allocations to 12, and
# `.map()` in a loop from 200 to none (#1045).
#
# What's left, after that and after wiring Rack and Pool into the drop pass:
#   - a container captured by a closure, or nested inside another container, is
#     never freed (#1035) — the biggest remaining share. A container in a
#     *struct field* is freed now (#1120): the handle a field read hands back
#     joins the struct's value group, so liveness knows the two die together
#   - a callback that builds a value per element — flat_map's inner sequences
#     and containers — hands back something nobody owns (#1045, the half this
#     didn't reach)
#   - a closure made on one branch of an `if` has no point where freeing it is
#     unconditionally safe (#1045)
#
# Eight files came off in one go when module-level consts started being freed:
# a const has no scope to end, so the drop pass had nowhere to put a release,
# and the frees now happen in a generated `rask_const_free` the runtime calls
# after main returns and after detached tasks are awaited (#1116). That covers
# the containers and boxes a const holds, and the heap block a `string` or
# struct const's slot points at.
#
# And 79 once a variable rewritten every turn of a loop freed what it held.
# `mut v = Vec.new()` before a loop that does `v = Vec.new()` inside it is one
# name holding a different vector each turn, and the drop pass is built around
# *values* — one allocation under several names, freed once. Union-find joined
# the phi to both its incoming values, so four vectors landed in one group and
# got one free. The phi is left out of every group now, which makes its
# definition the loop header and hands it to the back-edge rule that already
# exists for this. Where a name inside the loop copies the variable out, that
# copy owns the turn's value and gets the free instead — and the variable is no
# longer read as moved-away by that copy, because the path that leaves the loop
# never ran it (#1154, `t29_branch_merge.rk` 7 → clean).
#
# And 73 once `v.chunks(n)` said what its result holds. It builds a
# `Vec<Vec<T>>` and handed the outer vector a null element map, so the free
# gave back the array of handles and every chunk in it leaked. The element map
# already has a kind for "this element is a container" — the outer vector just
# never carried one (`t38_lazy_collection_adapters.rk` 6 → clean; the whole
# leak was that one test, not the adapter captures the old note claimed).
#
# And 63 once a pool freed what its elements own. A `Pool<Task>` whose `Task`
# holds a `tags: Vec<string>` gave back the slot array and the header and
# nothing else — a pool carried no element map at all, where a Vec, a Map and a
# rack node have had one for a while. It arrives with the first insert, the way
# a rack node's does: `Pool.new()` has no argument to read `T` off. A removed
# element is handed back to the caller and released by the frame, never by the
# pool — `Pool.remove` answers `T?`, so there is always somewhere to put it
# (`t_vec_field_filter_map.rk` and `t_with_collection_field_writes.rk` both
# clean). The earlier note here claimed the pool released a `remove` whose
# result nobody took; nothing generated ever takes that path.
#
# The element map was a struct-shaped question only, so `Pool<string>` and
# `Pool<Vec<_>>` described themselves as owning nothing: the element *is* the
# owned thing and no struct layout says so. The tag comes from lowering now,
# where the checker's type is, the same way a Vec's does — and `Pool.remove`
# says which container it hands back, so a removed `Vec` or `Map` is freed by
# the frame instead of nobody.
#
# And 57 once a struct read through a nested struct field still gave back its
# own containers. A struct and a struct *inside* it are one storage read at an
# offset, so the two share a release group — and the release landed on whichever
# name the last use touched, which for `p.home.street` is the inner `Address`.
# That walks a strict subset: the outer's `Vec` fields were released by nobody,
# and reading a *different* field of the same value was enough to make them come
# back. The release names the outermost member of the group now, which covers
# every field including the inner one (`t_json_decode.rk` 6 → clean; the leak
# was the one test that reads `p.home.street` and no Vec field).
#
# And 52 once an opaque handle in a wrapper was a container the pass could see.
# `s.to_cstring()` answers `cstring or NullByteError`, and a `cstring` is a heap
# buffer behind a handle MIR types `i64` — so the test for "is this slot a
# container", which asked for a pointer, missed the one `to_cstring` stores into
# the wrapper it returns, and every successful conversion leaked its copy. The
# store side asks whether the name is one this frame already owns a container
# under; the read side takes slot zero of a wrapper already known to carry one,
# where "one machine word" separates the payload from the error value beside it.
#
# The other half was a forwarder. `func terminated_copy_of(s) -> cstring or
# NullByteError { return s.to_cstring() }` makes nothing and stores nothing — it
# hands its caller the wrapper it was just given — so there was no name in it to
# read an answer off, and the answer is simply the callee's
# (`t_cstring_ffi.rk` 4 → clean, `t_container_in_a_wrapper.rk` clean).
#
# And 51 once an opaque handle in a struct field was freed with the struct. The
# table that says what frees a field by its type knew about containers and
# boxes; `Random`, `Atomic` and `cstring` are one-word handles onto a runtime
# block and were in none of them, so a `Random` in a field leaked its state on
# every construction. `StringBuilder` is deliberately left out — `build()` takes
# the builder away, so a release on the field would point at a block that is
# already gone (`t_week_opaque_struct_fields.rk` clean).
#
# And 45 with one more: `{v:debug}` had no line in `INTERNAL_SPELLINGS`, so
# reading a container handle off a struct field to render it looked like handing
# the whole struct away and the struct was never released
# (`t_debug_renders_containers.rk`).
#
# The other two that landed here were backed out. `h.list = h.list.filter(…)`
# leaves the vector it replaced to nobody, and freeing it at the assignment
# needs the struct's own release to be in the right place first — which it
# isn't: a store into one field was read as killing the *whole* aggregate, so
# `parse_args` in `examples/grep_clone.rk` released the struct's contents one
# statement after building it. That was silent until a second free landed at the
# write, and the coverage rule written to fix it (a block kills only when its
# stores cover every byte) put an intermittent SIGSEGV into `t_json_decode.rk`
# — 11 runs in 40, which is why every gate and every CI run passed it. Both are
# out again and #1198 is reopened with the measurement.
#
# And 41 with two more. `char_indices` was the third of the `chars` /
# `graphemes` family and the one left off the constructor list: it pushes a
# (byte offset, scalar) pair per character into a fresh vector, so the vector is
# the caller's and nothing in it points into the source
# (`t_string_unicode.rk` clean).
#
# And a box in a struct field. `Router { inner: Fail { … } }` moves the box in,
# so the block and the value's own contents are the struct's — but the release
# walk had no case for a trait object and skipped a struct whose only owning
# field was one. Two things were in the way: a field written `any Trait` reaches
# the layout as a *name* rather than a parsed TraitObject (#474), so asking for
# the parsed form answered no exactly where it mattered; and the release landed
# before the box's last use, because a field read only delayed it for a `T?` or
# a `T or E`. A trait object is the same shape as those — sixteen bytes read out
# of the struct's own storage, not a scalar copied out of it — and `r.inner`
# passed on to something else is read long after the read that produced it
# (`t62_trait_object_positions.rk` clean). The release goes through the
# runtime's own entry walker, which reads the vtable's hook; that hook is what
# separates it from `TraitDrop`, where the frame keeps the contents (#1144).
#
# And 39 once `drop(p)` on a `Heap<T>` gave back what the payload holds. The
# block was freed and the string and the `Vec` inside a `Heap<Record>` were not
# — and they live *in* the block, so after `rask_free` there is nothing left to
# walk. An aggregate local is an address, so a local typed as the payload and
# holding the block's pointer *is* the payload, and the ordinary contents
# release walks it. The rc pass skips a group something already placed a
# release for, read once before it starts adding its own
# (`t_heap_box_dropped.rk` clean).
#
# And 37 once the trait-drop pass followed a box as far as the frame carries it.
# Two gaps, both about a box that arrived from *somewhere else* rather than
# being made here. `describe(e)` inlined writes the box into the callee's
# parameter local, and only locally-made boxes were being carried through moves
# — so the original read as moved-away and the copy was never a candidate.
# And `quadrupled → doubled → classify` inlines into one body, so the box comes
# out of one wrapper, is parked in the next, comes out again, and is parked
# again: the step that follows one such hop ran once, which followed the first
# and lost the second. Both are fixed points now
# (`t_any_error_boxing.rk` and `t_bare_error_type.rk` clean).
#
# And 34 once an `own` capture of a refcounted handle took a reference (#1139).
# A `Sender<T>` is one word, so `spawn(own || …)` copied the handle and nothing
# bumped the channel's sender count: N tasks shared one sender. The pass had
# been conservative about it rather than wrong — a glue may not own a capture
# defined outside the loop that builds it, because every turn hands out the same
# value — so it leaked the handles instead of closing the channel under a task
# still sending into it.
#
# The clone goes at the create site, which is also what makes every rule here
# land without a special case: it is defined where the closure is built, so it
# is that capture's only capturer, it is made each turn, and `Sender_clone` is
# already a constructor the pass knows. Only where the frame still has the
# handle afterwards — `own` on the last use is a *move*, and
# `spawn(own || { … tx.close() })` depends on it: clone that one and the frame
# keeps a sender open, the receiver never learns the channel is done, and the
# file hangs instead of leaking (`t_channel_spawned_producer.rk` clean).
#
# And 32 once a closure writing through a captured variable gave back what that
# variable held. `seen = "{seen}…"` inside a `for_each` body captures `seen` by
# reference, so the write is a store through the frame's address
# (mem.closures/MC1): the new string was retained for the slot and the one it
# replaced was released by nobody (#1162).
#
# No aliasing guard, unlike the container case — a string is refcounted, so
# every name holds a count of its own and giving back the slot's is right
# whoever else is reading. And a capture points at a variable the frame
# established before building the closure, so a store through one is always a
# replacement.
#
# It is spelled `string_free_replaced` rather than an `rc_dec`, for the reason
# `Vec_free_replaced` is spelled apart from `Vec_free`: a dec on that name reads
# as "give back the reference this name took", a capture read takes none, and
# `rc_elide` strips it — correctly, and along with the one this wants. What is
# being released is the reference the *slot* holds, which is a different thing
# wearing the same local (`t_closure_heap_string_capture.rk` clean, and
# `t_capture_freed_with_its_closure.rk` 2 → 1: the other half of that one is
# #1200).
#
# Format: one basename per line, then `# N allocations`.

# The seventeen added 2026-09-05 came in with the bug-crunch branch: some
# are files that branch added, the rest leak there for shapes the fixes
# above didn't reach. None are new leaks — they were on that branch's own
# list before the merge.

t_shared_box_freed.rk  # 2 allocations, deferred — the handle `s.clone()` hands back.
                        # Not "held on purpose", which is what this line used to say:
                        # `clone_elision` may leave the caller's own box in the slot, so
                        # freeing the clone is a double free rather than a smaller leak.
                        # That is why `Shared_clone` isn't a registered constructor, and
                        # the file's own header says so. Zero is reachable — it waits on
                        # the elision knowing which box came back.

# A closure held in an optional, a tuple or a box — #1253. The frame drops one
# it holds by name and a container frees one it holds as an element (#1149); a
# value behind a tag, at a tuple offset, or inside a box is none of those. None
# of these shapes compiled before #1151, #1240, #1241 and #1242 landed, so the
# leak is what those fixes made reachable rather than anything they broke.
t_callable_out_of_wrapper.rk  # 2 allocations — closures out of an optional and a Map, #1253
t_function_type_in_a_slot.rk  # 8 allocations — closures in a box and in a tuple, #1253
